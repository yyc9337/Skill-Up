<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.surfinn.glzza.mybatis.mapper.MenuMapper">

<select id="selectMenuList" parameterType="com.surfinn.glzza.vo.MenuVO" resultType="com.surfinn.glzza.vo.MenuVO">
	SELECT
		MENU_SID,
		MENU_NM,
		MENU_EN_NM,
		MENU_URI,
		MENU_PSID,
		MENU_ORDER,
		DSCRPT,
		USE_YN,
		REG_DT,
		REG_ID,
		UPD_ID,
		UPD_DT
	FROM
		TB_MENU
	<where>
		AND USE_YN = '1'
	</where>
	ORDER BY MENU_ORDER ASC
		
</select>

</mapper>